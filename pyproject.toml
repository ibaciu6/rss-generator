[project]
name = "rss-generator"
version = "0.1.0"
description = "Config-driven RSS & Atom generator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

